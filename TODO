TODOs:
✓1. Create LightMountJoint
✓2. Create a (temporary) way to author a LightMountJoint
✓3. Implement LightMountSystem
✓4. Implement custom light editor
✓5. Implement Button, with contact detection
✓6. Implement ButtonLightController


Later TODOs:
1. Remove edge mounts past the contact point when calculating partial constraints
2. Make ShadowHitting objects choose to always resolve to light areas or dark areas.
3. Allow for overlapping shadow edges
4. Unbreak constraint gizmos
5. Move gizmo drawers behavior to PhysicsDebugger
